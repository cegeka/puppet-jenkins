import jenkins.*

<% if @enable_ssl %>
protocol = 'https://'
<% else %>
protocol = 'http://'
<% end %>

url = protocol + '<%= @jenkins_url %>'
admin_email = '<%= @admin_email %>'


jlc = new jenkins.model.JenkinsLocationConfiguration();
jlc.setUrl(url);
jlc.setAdminAddress(admin_email);
