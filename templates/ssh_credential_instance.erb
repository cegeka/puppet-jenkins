import java.io.File

def ssh_credential = evaluate(new File("/data/jenkins/init.groovy.d/ssh_credentials.groovy"))

ssh_credential.call(
    id = '<%= @name %>',
    username = '<%= @username %>',
    description = '<%= @description %>',
    keyfile = '/data/jenkins/.ssh/private_key_<%= @username %>'
)