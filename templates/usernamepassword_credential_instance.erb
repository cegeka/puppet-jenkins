import java.io.File

def ssh_credential = evaluate(new File("/data/jenkins/init.groovy.d/usernamepassword_credentials.groovy"))

ssh_credential.call(
    id = '<%= @name %>',
    secret_id = '<%= @pim_id %>',
    description = '<%= @description %>',
)